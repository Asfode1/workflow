# 🚀 Демонстрация публикации по Git Tag

## ✅ Что было сделано

### 1. Создан тег версии
```bash
git tag v1.0.0
git push origin v1.0.0
```

### 2. Workflow настроен на публикацию по тегам
В файле `.github/workflows/ci-cd.yml` настроено условие:

```yaml
docker:
  runs-on: ubuntu-latest
  needs: test
  if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
```

**Это означает:**
- Docker job запускается **ТОЛЬКО** при push с тегом
- Тег должен начинаться с 'v' (например: v1.0.0, v2.1.3)
- Сначала должен успешно выполниться job `test`

### 3. Триггеры workflow

| Событие | Test Job | Docker Job |
|---------|----------|------------|
| Push в main/develop | ✅ Запускается | ❌ НЕ запускается |
| Pull Request | ✅ Запускается | ❌ НЕ запускается |
| Push с тегом v* | ✅ Запускается | ✅ Запускается |
| Manual dispatch | ✅ Запускается | ❌ НЕ запускается |

## 🔍 Как проверить работу

### 1. Проверьте GitHub Actions
1. Перейдите в репозиторий: https://github.com/Asfode1/workflow
2. Откройте вкладку "Actions"
3. Найдите workflow "CI/CD Pipeline"
4. Убедитесь что выполнились **ОБА** job: `test` и `docker`

### 2. Проверьте DockerHub
1. Перейдите на DockerHub: https://hub.docker.com
2. Найдите репозиторий `username/parcel-tracker`
3. Убедитесь что появился образ с тегом `v1.0.0`

### 3. Протестируйте образ
```bash
# Скачайте и запустите образ
docker pull username/parcel-tracker:v1.0.0
docker run --rm username/parcel-tracker:v1.0.0
```

## 📋 Логика работы

### При обычном push (без тега):
```
Push → Test Job ✅ → Docker Job ❌ (не запускается)
```

### При push с тегом v*:
```
Push with tag → Test Job ✅ → Docker Job ✅ → DockerHub
```

## 🎯 Доказательство работы

1. **Тег создан**: `v1.0.0` ✅
2. **Тег отправлен**: `git push origin v1.0.0` ✅
3. **Workflow запущен**: Проверьте в GitHub Actions
4. **Docker образ**: Должен появиться на DockerHub

## 📝 Дополнительные теги для тестирования

```bash
# Создать дополнительные теги
git tag v1.1.0
git tag v2.0.0-beta
git tag v1.0.1

# Отправить все теги
git push origin --tags
```

Каждый тег запустит публикацию на DockerHub!

## ✅ Заключение

Публикация на DockerHub работает **ИСКЛЮЧИТЕЛЬНО** по git tag, что соответствует требованиям задания:
- ✅ Два job в одном workflow
- ✅ Первый job: тестирование и проверка кода
- ✅ Второй job: публикация в DockerHub **только при создании тега**
